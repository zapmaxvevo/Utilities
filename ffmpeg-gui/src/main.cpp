// wxIMPLEMENT_APP(App) in App.cpp generates the real main() / WinMain().
// This translation unit is intentionally empty; CMake lists it as the
// "entry point" file for clarity but the linker entry comes from App.cpp.
